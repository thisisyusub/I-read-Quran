name: i_read_quran
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter  
  flutter_svg: ^0.19.1
  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  google_fonts: ^1.1.1
  flutter_slidable: ^0.5.7
  scroll_to_index: any
  syncfusion_flutter_gauges: ^18.3.44
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^0.8.1
  build_runner: ^1.10.4

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
