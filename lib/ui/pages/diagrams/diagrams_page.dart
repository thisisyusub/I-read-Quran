import 'package:flutter/material.dart';

class DiagramPage extends StatelessWidget {
  const DiagramPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('DiagramPage'));
  }
}
